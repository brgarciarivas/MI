import React from 'react';

const ChartHeader = (props) => {
    
    return (
        <div className='ChartHeader'>
            {props.children}
        </div>
    );

}


export default ChartHeader;